// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sdl_image", "for sdl_image support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("sdl_image", "enable sdl_image support", "no");

if (PHP_SDL_IMAGE != "no") {
	EXTENSION("sdl_image", "sdl_image.c");
}

