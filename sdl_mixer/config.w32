// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sdl_mixer", "for sdl_mixer support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("sdl_mixer", "enable sdl_mixer support", "no");

if (PHP_SDL_MIXER != "no") {
	EXTENSION("sdl_mixer", "sdl_mixer.c");
}

